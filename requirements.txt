# Vercel выполняет `pip install -r requirements.txt` в корне репозитория.
# Ставим сам пакет 4caster из pyproject — он тянет все нужные зависимости
# (aiogram, fastapi, sqlalchemy, psycopg, numpy, ...). Тогда `import fourcaster`
# работает из site-packages, без бандлинга src/ и без блока functions.
.
